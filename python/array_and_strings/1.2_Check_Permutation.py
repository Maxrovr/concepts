# Given two strings, write a method to decide if one is a permutation of the other.

# Approach 1: sort the stings(R:O(nlogn)), then compare if they are the same(R:O(n)) {R: nlogn, S: 1}
# Approach 2: Count number of chars in each string, check if counts of both match (O) {R: n}

